name: Deploy Infrastructure

on:
  push:
    branches:
      - main
    paths:
      - 'infra/**'
      - '.github/workflows/deploy-infrastructure.yml'
  workflow_dispatch:
    inputs:
      image_tag:
        description: Container image tag to deploy
        required: false
        type: string
  workflow_call:
    inputs:
      image_tag:
        required: false
        type: string

permissions:
  id-token: write   # required for OIDC token request
  contents: read

jobs:
  deploy:
    name: Deploy Bicep infrastructure
    runs-on: ubuntu-latest
    environment: production
    outputs:
      aksClusterName: ${{ steps.bicep.outputs.aksClusterName }}
      containerRegistryLoginServer: ${{ steps.bicep.outputs.containerRegistryLoginServer }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Azure login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Resolve OIDC principal ID
        id: oidc-principal
        shell: bash
        run: |
          set -euo pipefail
          if ! principal_id="$(az ad sp show --id "${{ secrets.AZURE_CLIENT_ID }}" --query id -o tsv 2>/tmp/oidc-principal-error.log)"; then
            echo "Failed to resolve service principal object ID for AZURE_CLIENT_ID=${{ secrets.AZURE_CLIENT_ID }}"
            cat /tmp/oidc-principal-error.log
            exit 1
          fi
          if [ -z "${principal_id}" ]; then
            echo "Resolved an empty service principal object ID for AZURE_CLIENT_ID=${{ secrets.AZURE_CLIENT_ID }}"
            exit 1
          fi
          echo "principal-id=${principal_id}" >> "$GITHUB_OUTPUT"

      - name: Deploy Bicep template
        id: bicep
        uses: azure/arm-deploy@v2
        with:
          scope: resourcegroup
          resourceGroupName: ${{ secrets.AZURE_RESOURCE_GROUP }}
          template: infra/main.bicep
          parameters: >
            infra/main.bicepparam
            imageTag=${{ inputs.image_tag || 'latest' }}
            githubOidcPrincipalId=${{ steps.oidc-principal.outputs.principal-id }}
          failOnStdErr: true

  deploy-to-aks:
    name: Deploy to AKS
    needs: deploy
    uses: ./.github/workflows/deploy-to-aks.yml
    with:
      image_tag: ${{ inputs.image_tag || 'latest' }}
      aks_cluster_name: ${{ needs.deploy.outputs.aksClusterName }}
      container_registry_login_server: ${{ needs.deploy.outputs.containerRegistryLoginServer }}
    secrets: inherit
