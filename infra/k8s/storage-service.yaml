apiVersion: apps/v1
kind: Deployment
metadata:
  name: storage-service
  namespace: ghas-demo
  labels:
    app: storage-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: storage-service
  template:
    metadata:
      labels:
        app: storage-service
    spec:
      nodeSelector:
        kubernetes.azure.com/agentpool: workerpool
      containers:
        - name: storage-service
          image: ${ACR_LOGIN_SERVER}/storage-service:${IMAGE_TAG}
          imagePullPolicy: Always
          ports:
            - containerPort: 8082
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 512Mi
          readinessProbe:
            tcpSocket:
              port: 8082
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 8082
            initialDelaySeconds: 30
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: storage-service
  namespace: ghas-demo
spec:
  type: ClusterIP
  selector:
    app: storage-service
  ports:
    - port: 8082
      targetPort: 8082
